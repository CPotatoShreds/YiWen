# P3 · 合规（面向公网前）

- 状态：✅ 代码部分已完成（2026-09-13，按用户指示跳过备案类法定事项）
- 创建：2026-09-12
- 粒度：原为概要，2026-09-13 实现可代码化部分

## 执行记录（2026-09-13）

1. **用户协议与隐私政策页** ✅ `/terms`、`/privacy` 静态页（模板文本，页面内注明"正式运营前需法务审阅替换"）；注册页增加协议/隐私勾选（未勾选不可注册）。
2. **账号注销 + 数据导出** ✅
   - `DELETE /auth/me`：口令确认 → 软删匿名化（名号改"已注销_编号"、邮箱清空、口令随机化、`deleted_at` 落库）+ 全量吊销刷新令牌 + 清除会话 Cookie；
   - 登录与 `get_current_user` 拦截已注销账号（旧 access token 立即失效）；
   - `GET /auth/me/export`：个人数据 JSON 附件下载（资料/奇术/奇人/挑战全记录/进度）；
   - 前端：设置页"数据与账号"危险区（导出按钮 + 口令确认注销）。
   - 迁移：`d2e3f4a5b6c7` 含 `users.deleted_at`。
   - **产品边界（已决策并记录）**：注销保留创作内容、归属匿名化展示（推演详情对缺失用户的既有文案"已离席"逻辑天然兼容）。
3. **ICP 备案/内容安全评估** —— 按用户指示跳过，公开上线前需业务侧办理。
4. **邮件域名反垃圾（SPF/DKIM/DMARC）** —— 随邮件发信域名确定后由 DNS 配置解决（免费备用方案 QQ/163 SMTP 的配置指引见 `.env.example` 邮件段与 docs/plans/2026-09-12-hardening-p1.md 执行记录）。

## 验证
pytest 全量 60/60（新增覆盖：注销后旧 token 失效/旧名号登录拒绝/导出结构完整）。

## 执行记录
（后续调整在此追加）
